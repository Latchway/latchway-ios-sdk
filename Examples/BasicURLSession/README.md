# Basic URLSession

`BasicURLSession.swift` shows the transport boundary: authorize an ordinary
request, then use the SDK's cookie-free ephemeral URLSession. Replace the
identity provider and deployment values with application-owned values. No
upstream provider key is accepted or required.
